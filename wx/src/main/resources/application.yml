spring:
  profiles:
    active: dal, core, wx
  messages:
    encoding: UTF-8

server:
  port: 8082

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    org.mybatis: ERROR
    com.onlyforchris.store.wx: DEBUG
    com.onlyforchris.store: ERROR

swagger:
  production: false

mybatis:
  mapper-locations: classpath:/mapper/*.xml